'use strict';

import Reflux from 'reflux';

export default PPSToolActions = Reflux.createActions([
    // user actions
    'saveResultData',
    'deleteResultData',
    'deleteInputData',
    'saveInputData'
]);